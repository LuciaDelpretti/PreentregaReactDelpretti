# Configuración de build
[build]
  command = "npm run build"
  publish = "dist"

  [build.environment]
    NODE_VERSION = "22"

# Redirección importante para React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

